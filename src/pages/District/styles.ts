import styled from "styled-components";

export const Container = styled.div`
  margin: 0 auto;
  max-width:var(--page-width);

  .districtHeader{
    padding: 2.4rem 0;
    h2 {
      text-align:center;
    }
  }
`;